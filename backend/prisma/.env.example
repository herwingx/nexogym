# ──────────────────────────────────────────────────────────────────────────────
# GymSaaS — Variables de conexión directa para Prisma
# Copia este archivo como prisma/.env y rellena los valores para tu entorno.
#
# ¿Por qué existe un .env separado para Prisma?
# Prisma necesita una conexión DIRECTA a PostgreSQL (sin pgbouncer/pooler) para
# ejecutar migraciones, seeds e introspección. El servidor Express usa DATABASE_URL
# con pgbouncer para mejor performance en runtime.
# ──────────────────────────────────────────────────────────────────────────────

# Conexión directa a Supabase (puerto 5432, sin pgbouncer)
# Supabase lo llama "Direct connection" en Settings → Database
DIRECT_URL="postgresql://postgres:PASSWORD@db.PROYECTO_ID.supabase.co:5432/postgres"
